export default class GameObject{
    constructor(x, y, angle, scale, game)
    {
        this.game = game;
        this.x = x;
        this.y = y;
        this.angle = angle;
        this.scale = scale;   
    }
}